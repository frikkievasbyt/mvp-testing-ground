# Medical listings

This folder stores medical service provider cards in YAML format.

Naming convention:
- one file per provider
- lowercase slug, hyphen separated

Example:
- `george-mediclinic.yaml`
